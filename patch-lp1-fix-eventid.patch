diff --git a/src/components/LeadForm.astro b/src/components/LeadForm.astro
index e3a17a4..37fc199 100644
--- a/src/components/LeadForm.astro
+++ b/src/components/LeadForm.astro
@@ -331,8 +331,7 @@ const HeadingTag = headingTag as any;
 
       // Contexto de atribucion
       const stock = form.querySelector<HTMLInputElement>('[data-stock]')?.value ?? '';
-      const eventId = newEventId();
-      const ctx = collectContext(source, stock, eventId);
+      const ctx = collectContext(source, stock);
       for (const [key, value] of Object.entries(ctx)) {
         const hidden = form.querySelector<HTMLInputElement>(`input[type="hidden"][name="${key}"]`);
         if (hidden) hidden.value = String(value);
